@model ProjectNghiPhep.Models.tbl_per_relationship
@*@model List<demo16.Models.NHANVIEN>*@
@using (Html.BeginForm())
{
    <div class="container">
        <div class="form-group">
            <label>Mã nhân viên:</label>
            <input class="form-control" name="id_user_rel" placeholder="Enter id_per_rel" />
        </div>
        <div class="form-group">
            <label>Mã phân quyền</label>
            <input class="form-control" name="id_per_rel" placeholder="Enter id_per_rel" />
        </div>
        <div class="button">
            <button>
                Lưu
            </button>
        </div>
    </div>

    ///////////////////////////////////////////////////////
    @*<div class="container">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Họ & tên</th>
                    <th>Giới tính</th>
                    <th>Ngày sinh</th>
                    <th>Địa chỉ</th>
                    <th>Số điện thoại</th>
                    <th>Email</th>
                    <th>Chức vụ</th>
                    <th>User id</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count(); i++)
                {
                    <tr>
                        <td>@Model[i].MaNV</td>
                        <td>@Model[i].TenNV</td>
                        <td>@Model[i].GioiTinh</td>
                        <td>@Model[i].NgaySinh</td>
                        <td>@Model[i].DiaChi</td>
                        <td>@Model[i].SDT</td>
                        <td>@Model[i].Email</td>
                        <td>@Model[i].chucvu</td>
                        <td>@Model[i].id_user</td>
                    </tr>
                }
            </tbody>
        </table>

    </div>*@
    ///////////////////////////////////////////////////////
}

